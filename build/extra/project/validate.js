(function() {
  var Validate, log;

  log = require('de-logger');

  Validate = function(options) {
    var valid;
    valid = true;
    if (valid) {
      return options;
    }
  };

  module.exports = Validate;

}).call(this);
